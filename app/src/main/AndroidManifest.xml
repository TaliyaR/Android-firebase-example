<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firebaseexample">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.firebase_example.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.firebaseexample.ui.resetPass.ResetPasswordActivity" />
        <activity android:name="com.example.firebaseexample.ui.signIn.OtpActivity" />
        <activity android:name="com.example.firebaseexample.ui.signIn.SignInActivity" />
        <activity android:name="com.example.firebaseexample.ui.signUp.SignUpActivity" />
        <activity android:name="com.example.firebaseexample.ui.homePage.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8222219038832947~7695264525" />
    </application>

</manifest>
